This code is designed to convert a text file into a csv file that can be run through pyamaze to create a maze.

ProcessInput() is designed to process each line of the text file that you set when defining your Micromouse class. 
These values are put into a dictionary.
ProcessDict() is designed to sort the dictionary and write it to a csv value

CreatMaze() does everything on the pyamaze end and creates the maze. This part of the program isn't working.